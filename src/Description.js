
import product from './product.js';
// description of our product
const Description = ( ) => {
  return <h2 style={{color:'black',fontSize:'15px'}}>{product.description}</h2>;
};

export default Description;